defaults: &defaults
  docker:
    - image: circleci/node:8

version: 2
jobs:
  pre-build:
    working_directory: ~/gatsbyBlog
    <<: *defaults
    steps:
      - checkout:
          path: ~/gatsbyBlog/
      - attach_workspace:
          at: ~/gatsbyBlog/

      - restore_cache:
          key: node-v2-{{ checksum "package.json" }}-{{ arch }}

      - run: npm install

      - save_cache:
          key: node-v2-{{ checksum "package.json" }}-{{ arch }}
          paths:
            - node_modules

      - persist_to_workspace:
          root: ~/gatsbyBlog
          paths:
            - node_modules
  lint:
    working_directory: ~/gatsbyBlog
    <<: *defaults
    steps:
      - checkout
      - attach_workspace:
          at: ~/gatsbyBlog
      - run: npm run lint

  gatsby-deploy:
    working_directory: ~/gatsbyBlog
    docker:
      - image: yai111/circleci_awscli:0.1.0
    steps:
      - checkout:
          path: ~/gatsbyBlog/
      - attach_workspace:
          at: ~/gatsbyBlog
      - run:
          command: |
            CI=false npm run build:ci
            aws s3 sync build/ s3://gatsby-ci-demo  --region ap-southeast-2 --delete
          working_directory: ios

workflows:
  version: 2
  front-backend:
    jobs:
      - pre-build:
          filters:
            branches:
              only:
                - master
      - lint:
          requires:
            - pre-build
      - gatsby-deploy:
          requires:
            - lint
          filters:
            branches:
              only:
                - master
